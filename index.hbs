{{!< default}}

<div id="post-list">
{{#foreach posts}}
    <article class="{{post_class}}">
        <header>
            <h2><a class="post-title-in-list" href="{{url}}">{{{title}}}</a></h2>
        </header>
    </article>
{{/foreach}}
</div>